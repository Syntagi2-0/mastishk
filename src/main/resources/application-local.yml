spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/syntagi_lite}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

syntagi:
  security:
    jwt:
      secret: ${JWT_SECRET}

logging:
  level:
    com.syntagi: DEBUG
