spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

syntagi:
  security:
    jwt:
      secret: ${JWT_SECRET}

logging:
  level:
    com.syntagi: INFO
